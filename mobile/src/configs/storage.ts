const DATABASE_NAME = '@proffy';

export const HAS_COMPLETED_ONBOARDING = `${DATABASE_NAME}:has_completed_onboarding`;
export const SIGNED_IN_USER_TOKEN = `${DATABASE_NAME}:user_token`;
export const SIGNED_IN_USER = `${DATABASE_NAME}:user`;
export const FAVORITE_TEACHERS = `${DATABASE_NAME}:favorite_teachers`;
