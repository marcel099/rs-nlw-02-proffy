import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  subheader: {
    alignItems: 'center',

    backgroundColor: '#8257E5',

    paddingTop: 30,
    paddingBottom: 72,
  },
});
